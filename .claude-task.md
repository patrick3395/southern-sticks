Make a polish pass adding cool transitions, animations, and real functionality to the Southern Sticks site. Keep the existing design — just elevate it with motion and interactivity.

━━━━━━━━━━━━━━━━━━━━━━━
TRANSITIONS & ANIMATIONS
━━━━━━━━━━━━━━━━━━━━━━━

1. SCROLL-TRIGGERED FADE-INS (use Intersection Observer, no extra deps):
   - Create a custom React hook: hooks/useInView.ts
     - Returns a [ref, isInView] tuple
     - Options: threshold 0.15, triggerOnce: true
   - Wrap every major section in a div that starts opacity-0 translate-y-6 and transitions to opacity-100 translate-y-0 when in view
   - Stagger children in product grids: each card delays 0ms, 100ms, 200ms, 300ms

2. HERO TEXT ENTRANCE (components/Hero.tsx):
   - Each of the 3 lines ("PLAY", "WITH", "PRIDE") animates in sequentially on load
   - Start: opacity-0, translateX(-40px)
   - End: opacity-1, translateX(0)
   - Delays: 0ms, 200ms, 400ms
   - Use CSS animation classes added via useEffect after mount
   - The right-side image fades in with a slight scale from 1.05 → 1.0

3. NAVBAR:
   - On scroll past hero, navbar transitions from transparent/blended to solid white with shadow
   - Currently it goes transparent → white on scroll > 10px — make it start fully transparent when at top of page (the hero is dark green so white nav looks fine on dark bg)
   - Add a smooth transition duration-300 on the background-color change

4. PRODUCT CARDS (components/ProductCard.tsx):
   - On hover: image scales 1.06, card lifts with shadow-xl
   - Add a "QUICK VIEW" label that slides up from bottom of image on hover (absolute positioned, dark green bg, white text, translate-y-full → translate-y-0)
   - Gold underline on product name animates width 0% → 100%

5. MARQUEE:
   - Add pause-on-hover: when user hovers the marquee strip, animation pauses
   - CSS: .marquee-inner:hover { animation-play-state: paused; }

6. CATEGORY TILES (shop page / home categories):
   - On hover: image zooms 1.08, a thin gold border appears around the tile
   - Category name slides up slightly on hover (translateY -4px)

━━━━━━━━━━━━━━━━━━━━━━━
FUNCTIONALITY
━━━━━━━━━━━━━━━━━━━━━━━

1. WORKING SEARCH (components/Search.tsx — new):
   - Add a search icon (magnifying glass SVG) to the navbar between nav links and the SHOP NOW button
   - On click: an animated search bar expands from the icon (width 0 → 240px, transition)
   - Typing filters products from data/products.ts client-side
   - Results appear in a dropdown below: product image thumbnail (40px), name, price, category pill
   - Click result → navigates to /shop/[slug]
   - Press Escape or click outside → closes
   - Make this a "use client" component

2. PRODUCT IMAGE ZOOM (app/shop/[slug]/page.tsx):
   - On click of the product image, open a fullscreen lightbox overlay
   - Dark overlay, image centered, X button to close
   - Pressing Escape also closes
   - Smooth fade-in transition for the overlay
   - Make this a "use client" AddToCart component or ImageZoom component

3. SIZE SELECTOR IMPROVEMENT (app/shop/[slug]/AddToBag.tsx):
   - When a size is selected, the button text changes from "ADD TO BAG" to "ADD TO BAG — SIZE [S/M/L/XL]"
   - If no size selected, clicking ADD TO BAG shows a shake animation on the size selector row + small "Please select a size" message in gold
   - Size buttons: on hover show a subtle tooltip if size is "Low Stock" (just for S and XL as a demo)

4. EMAIL CAPTURE FORM (app/page.tsx):
   - The JOIN form should validate email format client-side
   - On submit: button shows a checkmark + "YOU'RE IN!" for 3 seconds, then resets
   - Input gets a green glow on valid email, red on invalid attempt to submit
   - This should be a "use client" component: components/EmailCapture.tsx

5. MOBILE MENU:
   - The hamburger → X transition should animate each line smoothly (already has CSS, verify it works)
   - Menu slides down with height animation (max-height 0 → max-height 500px, overflow hidden)
   - Add subtle backdrop when open

6. CART INDICATOR (optional but cool):
   - Add a cart icon (bag SVG) in the navbar with a small badge
   - Badge shows item count — store in localStorage via a simple useCart hook
   - When "ADD TO BAG" is clicked on product page, increment the count
   - Badge animates (scale bounce) when count increases
   - Create: hooks/useCart.ts (simple counter in localStorage)
   - Create: components/CartIcon.tsx

━━━━━━━━━━━━━━━━━━━━━━━
POLISH DETAILS
━━━━━━━━━━━━━━━━━━━━━━━

- Page transitions: add a thin gold progress bar at top of page that fills on navigation (like NProgress but CSS only — just a fixed top bar that animates width)
- Smooth scroll behavior: add `html { scroll-behavior: smooth; }` to globals.css
- Focus styles: replace default browser focus with a gold outline for accessibility
- Loading states: the shop filter tabs should have a subtle fade when switching categories
- Back to top: floating button appears after scrolling 400px, smooth scrolls to top, has gold bg and arrow icon

━━━━━━━━━━━━━━━━━━━━━━━
CRITICAL: IMAGE RULES
━━━━━━━━━━━━━━━━━━━━━━━
- ALL local /images/ paths MUST use img() helper: import { img } from "@/lib/image"
- Use plain <img> tags (not next/image) for local images
- Keep TypeScript clean

━━━━━━━━━━━━━━━━━━━━━━━
AFTER ALL CHANGES
━━━━━━━━━━━━━━━━━━━━━━━
Run: npm run build
If build has errors, fix them — do not skip.
If successful: npx gh-pages -d out --dotfiles
Then: git add -A && git commit -m "feat: transitions, animations, search, cart, lightbox, email capture" && git push origin main
Finally: openclaw system event --text "Done: Southern Sticks transitions and functionality pass deployed" --mode now
